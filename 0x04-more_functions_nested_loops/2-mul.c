#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply by the first number
 *
 * Return: the product of multiplying x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
